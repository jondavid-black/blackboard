# Behavior-Driven Development (BDD)

This directory contains Gherkin `.feature` files and step definitions for end-to-end testing.

**Framework**: `behave`
**Driver**: `playwright`
**Command**: `uv run behave`

**Structure**:
- `*.feature`: User stories and acceptance criteria.
- `steps/`: Python implementation of steps interacting with the Flet web view.
